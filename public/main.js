'use strict';

console.log(16);

addBtn.addEventListener('click', (e)=>{
    // console.log(toDoText.value);
    //
    // let xhr = new XMLHttpRequest();
    // xhr.open('GET', 'localhost:3000/form', true);
    // xhr.send();

}, );


// if( toDoText.value ) {
//   let li = document.createElement('li');
//   li.innerHTML = toDoText.value;
//   ol.appendChild(li);
// };
// toDoText.value = ''
